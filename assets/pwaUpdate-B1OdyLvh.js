async function e(){try{if(`caches`in window){let e=await caches.keys();await Promise.all(e.map(e=>caches.delete(e)))}if(`serviceWorker`in navigator){let e=await navigator.serviceWorker.getRegistrations();await Promise.all(e.map(e=>e.unregister()))}}catch{}let e=new URL(location.href);e.searchParams.set(`vc_cb`,String(Date.now())),location.replace(e.toString())}export{e as forceRefresh};